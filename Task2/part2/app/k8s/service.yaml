apiVersion: v1
kind: Service
metadata:
  name: scaletestapp
  labels:
    app: scaletestapp
spec:
  selector:
    app: scaletestapp
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080
      name: http-metrics